formatter: "markdown table"

sections:
  hide:
    - providers
    - resources

output:
  file: ../../docs/source/MODULE_CLUSTER.md
  mode: inject

content: |-
  {{ .Header }}

  {{ .Inputs }}

  {{ .Outputs }}  